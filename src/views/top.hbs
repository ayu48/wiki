<!DOCTYPE html>
    {{> head}}
    <body>
        {{> nav_bar}}
        <div class="container">
            <div class="container-fluid">
                <p class="bs-component pull-right">
                    <a href="/page/add" class="btn btn-success">Add Page</a>
                </p>
                <div class="page-header">
                    <h1>Pages</h1>
                </div>
                {{#each pages}}
                 <div>
                    <a href="/page/{{id}}">{{title}}</a>
                 </div>
                {{/each}}
            </div>
        </div>
    </body>
</html>
